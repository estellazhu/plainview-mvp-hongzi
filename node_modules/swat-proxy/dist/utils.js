'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
// If there's a trailing slash on the url, trim it
const trimSlash = exports.trimSlash = str => {
  if (str[str.length - 1] === '/') {
    str = str.substring(0, str.length - 1);
  }
  return str;
};